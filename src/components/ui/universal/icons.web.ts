import type { IconName } from '@expo/ui';

// Web renders text fallbacks, so native icon assets stay out of the web bundle.
export const LANGUAGE_ICON = null as unknown as IconName;
export const LIGHT_MODE_ICON = null as unknown as IconName;
export const DARK_MODE_ICON = null as unknown as IconName;
